const customerApi = require('./node-example-076');
const assert = require('chai').assert;

describe('Customer API module', () => {
    before(() => {
        // run before all tests in modele
    });
    beforeEach(() => {
        // run before each test in module
    });
    it('should run a test', () => {});
    afterEach(() => {
        // run after each test in module
    });
    after(() => {
        // run after all tests in module
    });
});

